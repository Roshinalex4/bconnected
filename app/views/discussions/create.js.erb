$("#discussions").prepend("<%= escape_javascript(render(@discussion)) %>");

/* Reset the comment form */
$("#start_discussion_form").hide();
/* Highlight the new comment */
$("#discussion_<%= @discussion.id %>").effect("highlight", {}, 3000);

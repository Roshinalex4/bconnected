Why not invite some people
<%= form_for @user_profile, :url => "/user_profiles/enter_password_to_invite/"+@user_profile.user_id.to_s, :html => {:method => :post} do |f| %>
	<%= f.text_field :email_assoc, :value => @user_profile.email_assoc %>
	<%= f.submit "Yes" %>
<% end %>

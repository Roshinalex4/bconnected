Connect with people you know on Bconnected

<%= form_for @user_profile, :url => "/user_profiles/show_email_contacts/"+@user_profile.user_id.to_s, :html => {:method => :post} do |f| %>
	<%= f.text_field :email_assoc, :value => @user_profile.email_assoc %>
	<%= password_field :email, :password %>
	<%= f.submit "Continue" %>
<% end %>

